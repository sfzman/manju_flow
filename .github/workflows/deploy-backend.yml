name: Deploy Backend to Alibaba Cloud SAE

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

env:
  REGION: cn-beijing

jobs:
  debug-change-order:
    runs-on: ubuntu-latest
    steps:
      - name: Install Alibaba Cloud CLI
        run: |
          curl -sSL https://aliyuncli.alicdn.com/aliyun-cli-linux-latest-amd64.tgz -o aliyun-cli.tgz
          tar -xzf aliyun-cli.tgz
          sudo mv aliyun /usr/local/bin/
          aliyun version

      - name: Configure Alibaba Cloud CLI
        run: |
          aliyun configure set \
            --profile default \
            --mode AK \
            --region "$REGION" \
            --access-key-id "$ALIYUN_ACCESS_KEY_ID" \
            --access-key-secret "$ALIYUN_ACCESS_KEY_SECRET"
        env:
          ALIYUN_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          ALIYUN_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}

      - name: Search SAE APIs
        run: |
          aliyun sae --help-search ChangeOrder || true
          aliyun sae --help-search Application || true

      - name: Describe SAE application state
        run: |
          set +e
          echo "### DescribeApplicationConfig"
          aliyun sae DescribeApplicationConfig --AppId "$SAE_APP_ID"
          echo "### DescribeApplicationImage"
          aliyun sae DescribeApplicationImage --AppId "$SAE_APP_ID"
          echo "### DescribeApplicationGroups"
          aliyun sae DescribeApplicationGroups --AppId "$SAE_APP_ID"
          echo "### DescribeApplicationInstances"
          aliyun sae DescribeApplicationInstances --AppId "$SAE_APP_ID"
        env:
          SAE_APP_ID: ${{ secrets.SAE_APP_ID }}
